{
	"name": "pagination-window",
	"version": "1.0.2",
	"description": "A window into a pagination list",
	"author": "Zeff Svoboda",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/AlteredConstants/pagination-window.git"
	},
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"clean": "rimraf dist",
		"build": "tsgo",
		"test": "bun test",
		"test:watch": "bun run test --watch",
		"format": "prettier --write .",
		"prepack": "bun run --sequential clean test build"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@typescript/native-preview": "^7.0.0-dev.20260428.1",
		"prettier": "3.8.3",
		"rimraf": "^6.1.3"
	}
}
